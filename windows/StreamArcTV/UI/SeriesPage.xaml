<ui:AppPage x:Class="StreamArcTV.UI.SeriesPage"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:ui="clr-namespace:StreamArcTV.UI">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Margin="12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <Button x:Name="BtnBack" Style="{StaticResource SquareIconButton}" ToolTip="Back">
                <Path Data="{StaticResource IconBack}" Fill="{StaticResource TextBrush}" Width="22" Height="22" Stretch="Uniform" />
            </Button>
            <TextBlock x:Name="TxtTitle" Grid.Column="1" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="14,0,0,0" TextTrimming="CharacterEllipsis" />
            <Button x:Name="BtnDownloadAll" Grid.Column="2" Content="Download all" Style="{StaticResource SmallButton}" Visibility="Collapsed" />
        </Grid>
        <Grid Grid.Row="1" Margin="16,0,16,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <Border Width="160" Height="230" Background="#0C1F3B" CornerRadius="8" ClipToBounds="True">
                <Image x:Name="ImgCover" Stretch="UniformToFill" />
            </Border>
            <TextBlock x:Name="TxtPlot" Grid.Column="1" Margin="16,0,0,0" FontSize="14" Foreground="{StaticResource TextMutedBrush}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="126" VerticalAlignment="Top" />
        </Grid>
        <ScrollViewer x:Name="Scroll" Grid.Row="2" VerticalScrollBarVisibility="Auto" Focusable="False" Margin="0,12,0,0" Padding="12,0,12,16">
            <StackPanel x:Name="List" />
        </ScrollViewer>
        <Control x:Name="Progress" Grid.Row="2" Style="{StaticResource Spinner}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
        <TextBlock x:Name="TxtError" Grid.Row="2" Foreground="{StaticResource DangerBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="24" TextWrapping="Wrap" Visibility="Collapsed" />
    </Grid>
</ui:AppPage>
